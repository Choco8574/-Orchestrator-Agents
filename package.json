{
  "name": "orchestrator-chatbox",
  "version": "1.0.0",
  "description": "Simple orchestrator chatbox proxying requests to the class-chat-model API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
